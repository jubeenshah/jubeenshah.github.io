# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Jubeen Shah
description: >- # this means to ignore newlines until "baseurl:"
  Passionately curious about technology
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://jubeenshah.github.io" # the base hostname & protocol for your site, e.g. http://example.com
image: /assets/images/og-card.png
twitter:
  username: jubeenshah
  card: summary_large_image
twitter_username: jubeenshah
github_username:  jubeenshah
linkedin_username: jubeen-shah

defaults:
  - scope:
      path: ""
    values:
      image:
        path: /assets/images/og-card.png
        height: 630
        width: 1200
        alt: Jubeen Shah Turbo Vision site preview
# Build settings
theme: minima
future: true
time: 2026-05-08 19:54:03 -0400
plugins:
  - jekyll-feed
  - jekyll-scholar

scholar:
  style: chicago-author-date
  locale: en
  source: ./_bibliography
  bibliography: references.bib
  sort_by: author
  order: ascending
  query: "@*"

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
 - .sass-cache/
 - .jekyll-cache/
 - gemfiles/
 - Gemfile
 - Gemfile.lock
 - node_modules/
 - vendor/bundle/
 - vendor/cache/
 - vendor/gems/
 - vendor/ruby/
